# Evidence

Artifacts from running this skill. Each run is a directory named `<utc-timestamp>-<hex>/`.

Cleanup of temp git repos **must not** delete this tree.

Typical layout:

```text
<run-id>/
  launch/
  doctor/
  drive/
  cleanup/meta.txt
```
