# Theme: Effective Patterns — Approaches That Worked

## Summary

Not all findings describe friction. These findings document techniques or pipeline configurations that worked well and are worth repeating. Capturing confirmed patterns prevents revisiting solved problems and gives future coordinators a validated playbook.

## Evidence

- [prfix multi-agent pipeline: haiku→opus→sonnet worked well for PR review](../../raw/2026-06-29T0000Z-5207ee28-workflow-prfix-multiagent-pipeline.md) — Running two scoped haiku agents (one per Codex finding) with TDD, then an opus agent expanding scope to find real issues the Codex review missed, then a sonnet agent implementing all findings: the 3-tier pipeline caught a meaningful API change (NewAttestation signature, FilterDiff, wired fingerprint/citation validation) that a single-pass fix would have missed. Sequential execution on the same branch was correct; parallel would have conflicted on the shared build system.
- [Sonnet reviewer with verbatim JSON template produced schema-valid assessments; haiku with prose prompt did not](../../raw/2026-06-29T1945Z-5207ee28-workflow-haiku-assessment-format-unreliable.md) — Sonnet reviewers given a concrete verbatim JSON template in the prompt (field names, bracket-notation criterion IDs, `schema_version: 1`) consistently produced valid ConformanceAssessment JSON on first attempt. Parallel haiku reviewers given the same task described in prose produced structurally invalid JSON requiring coordinator post-processing every time. The explicit template is the differentiating factor.
- [Parallel sonnet reviewers for independent tasks: no filesystem conflicts](../../raw/2026-06-29T1945Z-5207ee28-workflow-haiku-assessment-format-unreliable.md) — Three parallel review agents (T8/T9/T10) each writing to their own assessment file in the job tmp directory completed without interference. Read-only repo access + distinct output paths = safe parallelism for conformance review.
- [Haiku scope-creep caught by the review stage of its own pipeline](../../raw/2026-07-01-claude-workflow-prfix-multitier-chain.md) — A haiku fixer added an untested, unrequested extra commit step inside a migration function to satisfy its own new dirty-check; the follow-up opus review stage (not the original task scope) caught it since it wasn't covered by the original findings.
- [prfix reply-thread mismatch self-corrected within the same run](../../raw/2026-07-01T1500Z-claude-workflow-prfix-reply-thread-mismatch-self-corrected.md) — A second consecutive haiku→opus→sonnet cycle on the same PR caught and fixed a reply-thread targeting mismatch without user intervention, before the final commit/push/reply step.
- [Single-finding haiku→opus→sonnet chain ran clean end-to-end](../../raw/2026-07-01T2100Z-claude-workflow-prfix-single-finding-clean-chain.md) — With only one unresolved review thread, the full three-tier pipeline completed with no manual intervention.
- [GraphQL `reviewThreads` query, not REST comments, is the reliable way to check thread resolution state](../../raw/2026-07-02T1224Z-claude-workflow-prfix-multiagent-pipeline.md) — `gh api .../pulls/66/comments --paginate` returns all inline comments but no resolution status field and can return a large (150KB+) payload; the GraphQL `reviewThreads` query returns `isResolved`/`isOutdated` directly per thread, avoiding wasted classification work on already-resolved threads.
- [Thread-state check before dispatch avoids wasted classification work](../../raw/2026-07-02T1310Z-claude-workflow-prfix-single-finding-pipeline.md) — Checking resolved/unresolved state via GraphQL first found only 1 of 14 threads actually needed remediation, before any fixer agent was dispatched.
- [Two-finding haiku→fable→sonnet relay ran end-to-end without friction](../../raw/2026-07-02T2320Z-claude-workflow-prfix-two-finding-relay-clean.md) — Fable's scope-expanded review pass again outproduced the original bot reviewer, finding issues beyond the two literal findings.
- [Multi-model prfix pipeline on PR #69 worked end-to-end](../../raw/2026-07-04T2045Z-claude-workflow-prfix-multi-model-remediation-pr69.md) — All 3 unresolved threads classified correctly and fixed with real TDD; the rename in scope left minor legacy binding-file debt, caught and noted rather than silently shipped.
- [Second prfix pass on the same PR remained clean](../../raw/2026-07-05T0500Z-claude-workflow-prfix-second-pass-pr69-broad-review.md) — All three haiku fixers succeeded independently; the broad fable review pass again added value beyond the literal findings.
- [Second-pass broad review caught a bug the first-pass fix introduced](../../raw/2026-07-06T1330Z-claude-workflow-prfix-followup-review-caught-cross-platform-gap.md) — The first haiku fix correctly addressed the literal reviewer complaint (install PostToolUse hooks for one platform) but the follow-up broad review caught a cross-platform gap the narrow fix didn't cover.
- [Three-stage cascade surfaced bugs a narrow fix pass missed](../../raw/2026-07-06T1500Z-claude-workflow-prfix-cascaded-agents-caught-deeper-bugs.md) — The haiku fix agent correctly fixed the two bot-reported findings, but the necessarily-broader second reviewer pass, dispatched specifically to look beyond those two fixes, found additional real defects.
- [Stale LSP diagnostic nearly caused a false failure; second-pass review still earned its keep on a near-trivial diff](../../raw/2026-07-06T2000Z-claude-workflow-prfix-stale-lsp-diagnostic-and-thin-diff.md) — Even when only 1 of 5 review threads was unresolved and the resulting diff was thin, the broad review pass was still worth running — it's not just insurance for large diffs.

- [Holistic story-level review caught a critical bug both the per-task reviewer and the auditor missed](../../raw/2026-07-20T0330Z-claude-workflow-fable-review-caught-what-reviewer-missed.md) — TOPTIER-S4. A fable `/code-review` over the whole story diff found that `GitWorktreeBranches` swallowed every git failure and returned an empty map with nil error, so `PlanFixes` would misfire the missing-worktree remediation against *every* claimed issue from one transient git error. The armature-reviewer had assessed the task twice (yellow both times) without flagging it; the auditor's checks (citations, source freshness, outcome quality) structurally could not surface a runtime correctness bug.
- [Holistic post-integration review caught two real bugs in the delivery gate that per-task review missed](../../raw/2026-07-25T2330Z-claude-integration-scope-annotation-and-mergebase-bugs.md) — LNGHZN-S4. Per-task passes rated T1/T2 green/yellow, correctly given each diff in isolation. Only the whole-story diff exposed that `IsWithinScope` never strips the ` (new)` annotation that `arm create --scope` and every worker habitually append — meaning `ScopeContainmentCheck` would spuriously fail on essentially any task adding a new file. Invisible per-task because T1 transitioned to `done` before T2 wired the gate in at all.
- [Contract-mandated exact test names forced end-to-end tests that caught a silent, self-consistent path bug](../../raw/2026-07-23T2210Z-claude-validation-e2e-tests-caught-silent-path-bug.md) — LNGHZN-S3-T1. The original unit tests called the function directly and read the op back from a path the test computed the same wrong way the production code did, so they passed cleanly. Requiring six literal `_REQ_` test names forced tests driving the real CLI through `config.Context` resolution, which is what surfaced the divergence. The naming convention did real work here beyond bookkeeping.
- [First implement pass replaced a literal remediator slot with an unset variable](../../raw/2026-08-16T1819Z-5207ee28-workflow-prfix-adversarial-loop-caught-undefined-slot.md) — `/prfix` implement → adversarial review on PR #108. The implement pass turned `rem-${TASK_ID}` into an unset shell variable; the adversarial pass caught it before merge. Same cascade shape, now on skill-prose fidelity rather than a bot thread.

## Patterns

**Tiered model selection for PR review**: Use haiku for fast, scoped TDD fixes on well-defined findings; opus for semantic scope expansion (finding issues the initial review missed); sonnet for multi-file implementation that requires reasoning across the whole change. Running agents sequentially on a shared branch avoids build-system conflicts.

**Verbatim JSON template in reviewer prompts**: When dispatching a reviewer agent that must produce structured JSON, include the exact schema as a copy-paste template (not prose description). This is especially important for haiku but improves reliability for all models. The template should show field names, value format (e.g. `acceptance[0]` not `acceptance_0`), and required top-level fields (`schema_version: 1`).

**Parallel read-only reviewer dispatch**: Multiple reviewer agents can run in parallel safely when each writes to a distinct output file path and all repo access is read-only. Parallelism is safe; shared-branch write access is not.

**Fix → broad review → implement cascade reliably catches what the narrow fix misses**: This is now the single most repeatedly confirmed pattern across the findings pile — at least 10 independent `/prfix` runs across PRs #66, #69, #70, #71 (different tasks, different models filling the "broad reviewer" role — opus and fable both confirmed) all show the same shape: a fixer agent correctly and narrowly addresses the literal reviewer-flagged finding(s), and a second, deliberately-broader review pass (scope not limited to the literal findings) catches additional real defects — scope creep the fixer introduced, cross-platform gaps, silent-error classes, or issues the *original human/bot* reviewer missed entirely. This held even on a near-trivial single-thread diff, suggesting the value isn't proportional to diff size and the pass is worth running unconditionally, not just as insurance for large changes.

**GraphQL over REST for PR thread state**: `gh api .../pulls/<n>/comments` returns inline comments but no resolution status and can return oversized payloads; the GraphQL `reviewThreads` query returns `isResolved`/`isOutdated` directly, letting the orchestrator skip already-resolved threads before dispatching any fixer agent.

## Candidate Follow-Ups

- Encode the haiku→opus→sonnet pipeline as a recommended pattern in the `prfix` skill for PRs with more than 2 findings.
- Document the "sequential, not parallel" constraint for agents sharing a branch, alongside the existing guidance for parallel worktree-isolated agents.
- Add the verbatim JSON schema template to the armature-reviewer SKILL.md so it's available to any model dispatched as a reviewer.
- Make the "broad second-pass review, not just literal-finding fix" cascade the default `/prfix` shape rather than an option — the evidence across 4+ PRs no longer supports treating it as optional for small diffs.
- Document the GraphQL `reviewThreads` query as the preferred thread-state check in the `prfix` skill, ahead of REST comment listing.
- **Make a story-level holistic review a required step before opening a PR, not an optional extra pass.** Three independent stories (TOPTIER-S4, LNGHZN-S4, LNGHZN-S5) each had a real correctness bug that per-task review structurally could not see, because the bug lived in the *interaction* between tasks or only became reachable once a later task wired the earlier one in. This is the same blind spot the `2026-07-06` cross-task-format-drift finding documents and that F2's ADR names as an explicit non-goal ("scope-disjoint ≠ contract-safe") — per-task review inherits the same boundary. The coordinator skill should treat the whole-story diff as its own review unit.
