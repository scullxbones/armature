# In single-branch mode, state/ lives here and must not be committed.
# In dual-branch mode, state/ lives at the worktree root — see ../.gitignore.
# See architecture.md §2 (Directory Structure).
state/
